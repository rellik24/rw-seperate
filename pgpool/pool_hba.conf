# TYPE  DATABASE    USER        CIDR-ADDRESS          METHOD

# 允許本地連接
local   all         all                               trust
host    all         all         127.0.0.1/32          trust
host    all         all         ::1/128               trust

# 允許特定 IP 範圍的連接
host    all         all         192.168.1.0/24        md5

# 允許特定用戶從特定 IP 連接
host    all         postgres    192.168.1.100/32      md5

# 拒絕其他所有連接
host    all         all         all                   reject 